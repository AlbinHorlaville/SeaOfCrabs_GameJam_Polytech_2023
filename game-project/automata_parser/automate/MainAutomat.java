package automate;

import java.util.ArrayList;

/*public class MainAutomat {
	Automate A;
	
	MainAutomat(){
		State initial = new State();
		Condition C = new True();
		Action act = new Move();
		ArrayList<Action> L1 = new ArrayList<Action>();
		L1.add(act);
		Transition T = new Transition(initial, C, L1, initial);
		ArrayList<Transition> L2 = new ArrayList<Transition>();
		L2.add(T);
		initial.SetTransitions(L2);
		A = new Automate(initial);
	}
	
	public static void main(String args[]){
		MainAutomat MA = new MainAutomat();
		
		Entity e = new Entity();
		for (int i =0; i<100; i++) {
			MA.A.step(e);
			System.out.println(e.GetX());
		}
	}
}*/
